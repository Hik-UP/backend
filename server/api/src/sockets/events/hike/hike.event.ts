import { join } from './join.event';
import { move } from './move.event';
import { disconnect } from './disconnect.event';

const hikeEvents = {
  join,
  move,
  disconnect
};

export { hikeEvents };
